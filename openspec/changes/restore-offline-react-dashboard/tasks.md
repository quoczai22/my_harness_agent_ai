# Tasks

- [x] Install React, ReactDOM, and build tool (esbuild), implement React dashboard UI components in `src/ui/`, and configure local offline bundle build in `package.json`. Keywords: `React`, `ReactDOM`, `esbuild`, `bundle`, `src/ui`.
- [x] Update `src/dashboard.ts` to serve local React bundle and static assets alongside read-only APIs with 127.0.0.1 binding, segment-aware path containment, and no wildcard CORS. Keywords: `dashboard.ts`, `127.0.0.1`, `path.relative`, `isPathContained`, `CORS`.
- [x] Update `src/dashboard.test.ts` to verify zero-CDN offline operation, local React bundle serving, 127.0.0.1 binding, CORS restriction, read-only APIs, and passing `npm test`. Keywords: `dashboard.test.ts`, `npm test`, `React`, `offline`, `127.0.0.1`.

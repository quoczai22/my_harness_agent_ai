# Tasks

- [x] Implement bounded loopback probe helper `probeWorkspaceInstanceHealth` and update `getWorkspacesSummaryList` to return `instanceHealth` in `src/dashboard.ts`.
- [x] Update UI types in `src/ui/types.ts` and card rendering in `src/ui/App.tsx` with instance status badges (`ONLINE`, `OFFLINE`, `UNKNOWN`).
- [x] Add unit, integration, and security tests in `src/dashboard.test.ts` for online instance, offline instance, timeout handling, and SSRF prevention.
- [x] Run `npm test` and verify all tests pass.
- [x] Update handoff reports and request implementation review.


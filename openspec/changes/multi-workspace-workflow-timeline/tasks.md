# Tasks

- [x] Implement `extractWorkflowTimeline` helper and `GET /api/timeline` endpoint in `src/dashboard.ts`.
- [x] Extend `WorkspaceSummary` with `recentActivity` and integrate timeline in `src/dashboard.ts`.
- [x] Update UI types in `src/ui/types.ts` and add Timeline Card + card activity snippet in `src/ui/App.tsx`.
- [x] Add unit, integration, and security tests in `src/dashboard.test.ts` for timeline generation, sorting/limiting, malformed state resilience, and 403 unregistered workspace rejection.
- [x] Run `npm test` and verify all tests pass.
- [x] Update handoff reports and request implementation review.


# Tasks

- [x] Add the dashboard server and read-only workspace API; done when it serves validated workflow, Git, and allowlisted artifact data. Keywords: `dashboard-server`, `state.json`, `workspace-path`, `read-only`.
- [x] Add the React lane dashboard; done when it renders stage timeline, tasks, checkpoints, blockers, and idle state from the API. Keywords: `React`, `workflow-lane`, `checkpoint`, `blockers`.
- [x] Add dashboard API and UI verification; done when test/build commands pass against fixture and real workspace data. Keywords: `dashboard.test`, `npm test`, `fixture`, `API`.
